 p= mat +1; // expressão válida, pois mostra o endereço do segundo elemento o vetor;
 p = mat++; //expressão inválida, pois não é possível alterar o endereço do vetor;
 p = ++mat; // expressão inválida, pois não é possível alterar o endereço do vetor;
 x = (*mat)++; // expressão válida, retorna o conteúdo da posição do vetor incrementado em uma unidade;